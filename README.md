# python-scripts
A collection of python scripts

## scripts
### "sort-into-folders-by-file-extension.py"
A simple script to sort files from an input folder into an output folder, based on file extension.


| script | description |
| ------ | ----------- |
| sort-into-folders-by-file-extension.py | A simple script to sort files from an input folder into an output folder, based on file extension. |
